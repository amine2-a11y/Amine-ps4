# 11.02 WebKit — Gold Theme

Updated visual theme for the existing WebKit project.

### Changes
- New black + metallic-gold visual style.
- Supplied PS4 background added at `includes/assets/backgrounds/ps4-bg.jpg`.
- Gold accent, buttons, focus states, tabs, scrollbar and progress bar.
- Gold theme is available as `11.02 Gold` in the theme selector.
- Gold is the default color for a fresh browser session.
- Existing WebKit/exploit JavaScript and payload files were left unchanged.

This is a visual/theme update only; it does not create or modify a PS4 executable.
