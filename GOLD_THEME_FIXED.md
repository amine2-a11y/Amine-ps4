# 11.02 WebKit — Gold Edition (Fixed)

This build forces the Gold color theme on first load and includes the PS4 background at:
`includes/assets/backgrounds/ps4-bg.jpg`.

The page also has an inline fallback so the gold theme/background appears even if an old cached stylesheet is present.
